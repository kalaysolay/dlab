# Design QA — кликабельный экран урока

- Source visual truth: `C:\Users\admin\IdeaProjects\damulab\redesign\lessons\variant-1.png`
- Source pixels: 1487 × 1058, RGB.
- Implementation: `http://127.0.0.1:4173/lessons/`
- Implementation screenshot evidence: Codex in-app Browser capture in the current task (desktop viewport 1440 × 1024 and mobile viewport 390 × 844).
- CSS viewport / density: 1440 × 1024 at browser default density; mobile check at 390 × 844.
- State: это историческая QA прототипа; он показывал оба режима вкладок. Для production выбран только non-sticky режим, закрепление не переносилось.

## Full-view comparison evidence

Выбранный вариант задаёт визуальную систему, общую шапку, двухсекционную навигацию урока и два режима поведения вкладок. Реализация сохраняет эти элементы и сознательно расширяет вкладку «Теория» по уточнению пользователя: вместо короткой карточки добавлены длинный учебный материал, содержание, крупные формулы и медиавставки.

Компоновка, ширина оболочки, мягкий фон, белые поверхности, indigo/green-состояния, скругления и плотность управляющих элементов соответствуют текущему приложению и исходному направлению.

## Focused region evidence

- Формулы: inline-значения увеличены относительно основного текста; правило и разобранный пример используют отдельную крупную математическую шкалу.
- Изображения: реальные SVG текущего приложения встроены по ходу текста и открываются в модальном просмотре без растрирования.
- Медиа: видео и GIF представлены внутри лекции; клик открывает просмотр поверх текущего места чтения.
- Тест: MCQ, FILL_IN и MATCHING, отвеченные/текущий/неотвеченные номера и кнопки «Назад» / «Дальше» проверены интерактивно.
- Урок без теста: вкладка и CTA тестирования скрываются; после конспекта появляется пояснение и кнопка «Завершить урок».
- Поведение вкладок: prototype позволял сравнить sticky и non-sticky; в production зафиксирован только non-sticky режим.

## Findings

P0/P1/P2 несоответствий после исправлений не осталось.

### Comparison history

1. P2 — часть управляющих иконок была пустой, потому что прототип ссылался на символы, которых нет в текущем `lucide-sprite.svg`.
   - Fix: оставлены только существующие иконки Damulab; стрелки и закрытие заменены ясными текстовыми действиями.
   - Post-fix evidence: повторный desktop capture показывает иконку теории, навигацию и медиакнопки; консоль браузера не содержит ошибок и предупреждений.
2. P2 — системный focus-outline визуально выбивался из макета.
   - Fix: добавлен доступный indigo focus-ring с отступом.
   - Post-fix evidence: повторный desktop capture после reload.

## Required fidelity surfaces

- Fonts and typography: системные Manrope/Nunito-подобные стеки повторяют текущее приложение; заголовки, основной текст, подписи и формулы имеют различимую иерархию.
- Spacing and layout rhythm: desktop-сетка 218px + 720px, стабильные межсекционные интервалы и адаптация до одной колонки на узком экране.
- Colors and visual tokens: indigo primary, green answered-state, slate text, pale borders and muted surfaces согласованы с текущим интерфейсом.
- Image quality and asset fidelity: используются исходные SVG урока из приложения; модальный просмотр сохраняет векторное качество.
- Copy and content: русский учебный материал согласован между теорией и тестом; отдельный RU / KK-переключатель внутри урока отсутствует.

## Primary interactions tested

- «Теория» ↔ «Тестирование»;
- выбор ответа и обновление счётчика 2/6 → 3/6;
- заполнение FILL_IN и полного MATCHING с обновлением счётчика до 4/6;
- переключение на урок без теста: вкладка и кнопка перехода скрыты, «Завершить урок» показана;
- компактная панель номеров вопросов проверена на мобильном viewport;
- переход «Дальше» к вопросу 4;
- открытие и закрытие изображения;
- prototype-сравнение sticky/non-sticky; production-решение — только non-sticky;
- desktop 1440 × 1024 и mobile 390 × 844;
- console errors/warnings: 0.

## Follow-up polish

- P3: заменить демонстрационный видео-экран реальным учебным роликом, когда будет выбран контент.
- P3: подключить настоящую GIF-анимацию вместо статического превью, когда появится исходный файл.

## Production reader — этап 2

- Source visual truth: `C:\Users\admin\IdeaProjects\damulab\redesign\lessons\variant-1.png`, правая (non-sticky) версия с учётом последующих решений убрать прогресс и `Сохранить место`.
- Implementation: `http://127.0.0.1:18080/student/lectures/1` на локальном test-профиле.
- Desktop capture: Codex in-app Browser, целевой viewport 1440 × 1024.
- Mobile check: browser viewport 390 × 844; breakpoint-правила находятся в существующем `app.css`, вкладки и содержание остаются в normal flow.
- Проверены две вкладки, активные/скрытые панели, keyboard/ARIA-состояния, CTA из теории, оглавление по реальным `h2/h3` и hash anchors.
- При выборе SCQ-ответа, переходе в теорию и возврате в тест выбранное значение сохранилось: панели не перемонтируются и форма не перезагружается.
- В тестовой опубликованной лекции присутствовали реальные SCQ, MCQ, MATCHING и FILL_IN из текущего backend.
- Console errors/warnings: 0.
- P0/P1/P2 расхождений в пределах этапа 2 не найдено. Типографика Quill/media относится к этапу 3, пошаговый navigator — к этапу 4, финальные completion-состояния — к этапу 5.

final result: passed

## Production reader — финальная проверка этапов 3–6

- Source visual truth: `C:\Users\admin\IdeaProjects\damulab\redesign\lessons\variant-1.png`, правая non-sticky версия с последующими решениями убрать progress/`Сохранить место` и не добавлять `Шпаргалку` в V1.
- Implementation: `http://localhost:18080/student/lectures/1`, локальный test-профиль с реальными Thymeleaf/Spring MVC формами.
- Проверен реальный опубликованный урок с SCQ, MCQ, MATCHING и FILL_IN.
- Desktop: вкладки, один вопрос, компактный navigator, зелёные отвеченные номера, синий текущий номер, `Назад`/`Дальше` и `Завершить тестирование` соответствуют выбранному варианту.
- Incomplete state: при результате `3/4` форма не отправилась; показаны `Остались вопросы`, номер 4 и `Вернуться к вопросам`. Текста о завершённом тесте нет.
- Success state: правильные значения прошли серверный `AnswerChecker`; показаны `Тест сдан`, `Верно: 4 из 4`, затем `Изучение завершено`.
- No-test state проверен до финального restart: тестовой вкладки и CTA перехода нет, отображаются согласованный текст и `Завершить урок`.
- Mobile 390/320 px: вкладки находятся в normal flow. На 320 px полный текст `Тестирование` сохраняется за счёт скрытия дублирующего badge-счётчика в заголовке; счётчик остаётся внутри navigator.
- Browser QA нашёл промежуточное отображение `Отвечено answered из total`; маркеры заменены на безопасные для MessageFormat, добавлена интеграционная регрессия, повторная браузерная проверка показывает `Отвечено 0 из 4`.
- Browser console errors/warnings: 0.
- Полный `gradlew test --no-daemon`: BUILD SUCCESSFUL.
- P0/P1/P2 расхождений после исправлений не осталось.

final result: passed
